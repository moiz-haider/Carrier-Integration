# Carrier Integration Service - Environment Variables
# Copy to .env and fill in values. Never commit .env.

# --- UPS Carrier ---
# Optional; defaults to https://onlinetools.ups.com if not set
UPS_BASE_URL=https://onlinetools.ups.com
# Leave empty for local dev (app starts; rate calls will fail with AUTH_FAILED until set)
UPS_CLIENT_ID=your_ups_client_id
UPS_CLIENT_SECRET=your_ups_client_secret

# Optional: override OAuth token URL (default: UPS_BASE_URL/security/v1/oauth/token)
# UPS_OAUTH_TOKEN_PATH=/security/v1/oauth/token

# Optional: request timeouts (milliseconds)
HTTP_TIMEOUT_MS=15000
HTTP_RATE_REQUEST_TIMEOUT_MS=20000

# Optional: for local/dev without real credentials (tests use stubs regardless)
# NODE_ENV=development
